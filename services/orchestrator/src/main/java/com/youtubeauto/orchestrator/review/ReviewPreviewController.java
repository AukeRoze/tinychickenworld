package com.youtubeauto.orchestrator.review;

// Retired. The /review/* server-rendered preview pages were replaced by the
// static UI (/ui/job.html). The scene-image endpoint the new UI uses moved to
// MediaController, and review emails now link to /ui/job.html?id=... instead.
// This file is intentionally empty so the old preview routes no longer register.
